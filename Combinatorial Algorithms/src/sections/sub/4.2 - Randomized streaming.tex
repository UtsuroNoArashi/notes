\documentclass{subfiles}
\begin{document}\label{sec:randomized-streaming}
    More often than not, a deterministic streaming algorithm cannot compote the quantity of interest 
        in exactly sub-linear time. It is for this reason that most streaming algorithm use randomization.

    In what follows we present a simple randomized streaming algorithm,
        showing its correctness and its spatial complexity.
        Such an analisis requires some notion of probability theory,
        which we assume the reader to own.

    \subsubsection{The median problem}
    \subfile{../subsub/4.2.1 - The median problem}
\end{document}
